# NNR8 Pulled Source Evidence

Temporary extraction root used for TeX/CSV inspection:

```text
C:\Users\admin\AppData\Local\Temp\aasc_nnr8_sources
```

This note records local source material pulled from the corpus, keyed to the
remaining fields of:

```lean
AASCNNR8CoreSourceProofBundle C M H N S
```

## CS6C: Boundary-Kernel Reconstruction

Source archive:

```text
G:\AASC corpus may 7\Core spine\ZZZNew Work\ZSubmission versions\Target\CS ARC\CS6C\tex\Neutrino_Oscillation_as_Boundary_Kernel_Reconstruction.zip
```

Extracted source:

```text
C:\Users\admin\AppData\Local\Temp\aasc_nnr8_sources\Neutrino_Oscillation_as_Boundary_Kernel_Reconstruction\CS6C_Neutrino_Oscillation_Boundary_Kernel_Reconstruction.tex
```

Evidence:

| Field target | Source location | Extracted content |
| --- | --- | --- |
| `noUncarriedSameTargetConstraint`, `noLegacyTheoryOutsideCensus` | line 425, theorem `Same-scope oscillation rival exhaustion` | Every same-scope rival to the boundary-kernel reconstruction is bookkeeping-equivalent, inadmissible, or scope-changing. No independent transported-flavor reconstruction survives. |
| `noScopeChangingRouteCountsSameScope` | line 490, OSC-C7 table row | Sterile/BSM extensions have status `LargerScope`; they are not same-scope rivals unless witness-equivalent. |
| `noScopeChangingRouteCountsSameScope` | `cs6c_registry.csv`, line 8 | `OSC-C7, sterile/decoherence/collapse/BSM extension, enlarged scope unless witness-equivalent, depends on new scope, LargerScope, ..., not same-scope rival`. |

Use:

```text
CS6C-THM-006 clears same-scope rival exhaustion.
CS6C-REG-007 clears scope-changing route exclusion.
```

## CS7: Cross-Sector Registry Controls

Source archive:

```text
G:\AASC corpus may 7\Core spine\ZZZNew Work\ZSubmission versions\Target\CS ARC\CS7\tex\Cross_Sector_Claim_Registry_and_Reportability_Classification.zip
```

Extracted directory:

```text
C:\Users\admin\AppData\Local\Temp\aasc_nnr8_sources\Cross_Sector_Claim_Registry_and_Reportability_Classification
```

Evidence:

| Field target | Source location | Extracted content |
| --- | --- | --- |
| `crossCalibrationFree`, `noEmpiricalValueImportAsRoute` | `forbidden_promotions.csv`, line 10 | `PMNS invariant witness to numerical PMNS values, Deferred`. |
| `noEmpiricalValueImportAsRoute` | `deferred_targets.csv`, line 14 | `PMNS values, Deferred, Invariant witnesses certified but values not derived, PMNS numerical theorem`. |
| `crossCalibrationFree`, `noEmpiricalValueImportAsRoute` | `final_reportable_claims.csv`, lines 5-6 | PMNS boundary kernel and PMNS invariant witnesses are certified structural claims only; values are deferred. |
| `crossNoHiddenSelector` | `forbidden_promotions.csv`, line 12 | `Global compatibility to singleton extraction, Forbidden`. |

Use:

```text
CS7-FORB-001 and CS7-DEFER-001 clear numerical-import firewall fields.
```

## ROC: Role-Occupancy Closure

Source archive:

```text
G:\AASC corpus may 7\Core spine\ZZZNew Work\ZSubmission versions\Erkenntnis\Role-Occupancy Closure under Admissibility\tex\Role_Occupancy_Closure_under_Admissibility.zip
```

Extracted source:

```text
C:\Users\admin\AppData\Local\Temp\aasc_nnr8_sources\Role_Occupancy_Closure_under_Admissibility\Role_Occupancy_Closure_under_Admissibility.tex
```

Evidence:

| Field target | Source location | Extracted content |
| --- | --- | --- |
| `currentPointIdentification`, role closure | line 520, theorem `Role-Occupancy Closure under Admissibility` | For a role-realization-complete certificate, realization is licensed exactly when Role-Occupancy Closure holds. |
| `noUncarriedSameTargetConstraint`, `noLegacyTheoryOutsideCensus`, `kernelForcedUEAPFailure` | line 594, theorem `Exhaustion of lawful closure routes` | A closure failure can be resolved only by lawful routes: impossibility elimination, presentation quotient collapse, source-witness construction, target-role revision, or lawful scope reset. |
| `crossNoHiddenSelector`, `noEmpiricalValueImportAsRoute` | line 296, role-insertion definition context | Role insertion may not be supplied by likelihood ranking, success, elegance, posterior comparison, preference ordering, scalar proximity, delayed calibration, or post-hoc repair. |
| `currentPointIdentification` | line 834, conclusion | The singleton case is recovered when the forced target role set has exactly one occupant; cardinality equality alone is only a shadow. |

Use:

```text
ROC-THM-001 supports current-point role occupancy.
ROC-THM-002 supports lawful route exhaustion and no-eleventh completeness.
ROC-NON-001 supports no empirical tie-break.
```

## NOAR: Unique Admissible Reconstruction

Source archive:

```text
G:\AASC corpus may 7\Core spine\ZZZNew Work\ZSubmission versions\Erkenntnis\Neutrino Oscillation as the Unique Admissible\tex\Neutrino_Oscillation_as_the_Unique_Admissible_Reconstruction.zip
```

Extracted source:

```text
C:\Users\admin\AppData\Local\Temp\aasc_nnr8_sources\Neutrino_Oscillation_as_the_Unique_Admissible_Reconstruction\source\main.tex
```

Evidence:

| Field target | Source location | Extracted content |
| --- | --- | --- |
| `crossNoOvercounting`, `currentPointIdentification` | line 462, corollary `Universal quotient form of oscillation uniqueness` | The finite witness-complete noncommuting PMNS domain has a single nontrivial coherent reconstruction object represented by the boundary kernel. Any alleged second object maps to the same quotient object, violates partial-domain preservation, or changes physical scope. |
| `noLegacyTheoryOutsideCensus`, `noUncarriedSameTargetConstraint` | line 859, lemma `No hidden flavor transport` | Hidden flavor transport preserving the same coherent PMNS witness is bookkeeping-equivalent to the boundary kernel, an inadmissible hidden selector, or a scope change. |
| `crossTransportCoherent` | line 784, theorem `Boundary trace does not imply interior identity` | Boundary flavor trace fixes a boundary anchor, not an interior identity carrier, unless an admissible transport invariant carries the label through propagation. |

Use:

```text
NOAR-COR-001 supports quotient uniqueness / no overcounting.
NOAR-LEM-001 supports no hidden transported-flavor route.
NOAR-THM-002 supports boundary-vs-interior transport discipline.
```

## MR3: Spectral and Hierarchy Sources

Source archive:

```text
G:\AASC corpus may 7\Core spine\ZZZNew Work\ZSubmission versions\Erkenntnis\Metric Relative ARC\MR3 Spectral and Hierarchy Sources of Metric_Relative Shape\TEX\Spectral_and_Hierarchy_Sources_of_Metric_Relative_Shape.zip
```

Extracted source:

```text
C:\Users\admin\AppData\Local\Temp\aasc_nnr8_sources\MR3_Spectral_and_Hierarchy_Sources_of_Metric_Relative_Shape\main.tex
```

Evidence:

| Field target | Source location | Extracted content |
| --- | --- | --- |
| all `MR3SpectralSourceAdmissionWitness` fields | `main.tex`, abstract line 55 | A candidate source is admitted only when it is carrier-attached, quotient-stable, transport-disciplined, calibration-free, extraction-certified, invariant under admissible source redescription, and stable under admissible extraction. |
| `sourceCertified`, `sourceInducesShapeMap` | `main.tex`, definition around lines 204-237 | Defines source candidates and the source-induced shape map from admitted source certificates to domain-indexed restrictions on the admissible shape fiber. |
| `quotientStable`, no duplicate source counting | `main.tex`, line 279 | MR3 individuates sources by admitted quotient image; different realizations inducing the same domain-indexed ratio ledger or shape-fiber restriction count as equivalent sources for collapse accounting. |
| `calibrationFree` | `main.tex`, criterion line 344 | A spectral or hierarchy source is calibration-free only if its source certificate survives deletion of target numerical table, target values, target-derived proxies, learned surrogates, boundary conditions selected to recover the target, and optimization targets constructed to reproduce it. |
| `standingSpectralCarrier`, `quotientStable`, `transportClosed`, `calibrationFree`, `extractionCertified` | `main.tex`, theorem line 355, `Spectral carrier admission theorem` | A spectral candidate is MR3-admitted only if the carrier is standing-bearing, the spectral operation is quotient-stable, entries are not basis artifacts, calibration ancestry survives empirical deletion, and extraction is certified. |
| `transportClosed`, source-standing discipline | `main.tex`, lines 178-183 | Transport standing concerns preservation of quotient-level relational content under admissible scale movement; representational running is not enough. |
| `calibrationFree`, no fitted exponents | `main.tex`, theorem line 530, `Exponent standing theorem` | A hierarchy exponent has MR3 standing only if forced by a standing-bearing quotient relation, not selected to fit an observed ratio. |
| `calibrationFree`, no dimensionless promotion | `main.tex`, theorem line 550, `Dimensionless non-promotion theorem` | A dimensionless relation is not MR3-admitted merely because it lacks units; it must satisfy carrier attachment, quotient stability, transport typing, calibration deletion, extraction certification, and shape-fiber relevance. |

Use:

```text
AASC-MR3-THM-010 and supporting MR3 definitions clear the MR3 witness fields.
```

## Source Rows Not Yet Pulled As Text

These are present as PDFs or already formalized Lean material, but `pdftotext`
is not available in this environment, so exact source text was not extracted
from these PDFs during this pass:

| Source row | Local file |
| --- | --- |
| `AASC-EX8-THM-11-2` | `G:\AASC corpus may 7\Core spine\ZZZNew Work\ZSubmission versions\Erkenntnis\Exactness ARC\EX8 Cross-Sector Exactness by Overconstraint\EX8__Cross_Sector_Exactness_by_Overconstraint.pdf` |
| `AASC-MR5-PROP-002` | `G:\AASC corpus may 7\Core spine\ZZZNew Work\ZSubmission versions\Erkenntnis\Metric Relative ARC\MR5 Cross-Sector Shape Collapse\Cross_Sector_Shape_Collapse.pdf` |
| `UEAP-THM-001`, `UEAP-THM-002` | `G:\AASC corpus may 7\Core spine\ZZZNew Work\ZSubmission versions\Erkenntnis\UEAP\Claim_Standing_and_Legitimacy.pdf`; Lean formalization also exists in `MaleyLean/Papers/ClaimStandingAndLegitimacy/PaperStatements.lean`. |

## Immediate Field Coverage

Pulled source text strongly supports:

```text
H.crossTransportCoherent
H.crossNoOvercounting
H.crossCalibrationFree
H.crossNoHiddenSelector

N.noUncarriedSameTargetConstraint
N.noLegacyTheoryOutsideCensus
N.noExperimentalMethodOutsideCensus
N.noScopeChangingRouteCountsSameScope
N.noEmpiricalValueImportAsRoute

currentPointIdentification
```

After extracting the MR3 TeX archive, every field of the current core source
bundle has at least one local source candidate with text-level evidence.

## Lean Source-Coverage Ledger

The source-availability layer is now represented in Lean by:

```lean
inductive AASCNNR8CoreSourceID
inductive AASCNNR8CoreFrontierField
def primarySourceForCoreFrontierField
theorem everyCoreFrontierFieldHasPulledSource
theorem coreSourceCoverageCleared
```

This clears the question:

```text
Does every remaining frontier field have a pulled corpus source candidate?
```

The answer is yes, formally:

```lean
AASCNNR8CoreSourceCoverageCleared
```

The remaining blocker is no longer source availability. It is semantic
extraction: converting the cited source passages into proof terms for the
corresponding propositions in `AASCNNR8CoreSourceProofBundle`.
