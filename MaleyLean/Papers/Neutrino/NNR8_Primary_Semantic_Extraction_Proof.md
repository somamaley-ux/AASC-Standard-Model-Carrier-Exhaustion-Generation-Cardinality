# NNR8 Primary Semantic Extraction Proof

Target Lean object:

```lean
AASCNNR8PrimarySemanticExtractionCertificate C M H N S
```

This proof note explains how the pulled corpus sources discharge the fields of
the primary semantic extraction certificate. It is a paper-level proof of the
certificate. Full Lean inhabitance requires the cited source theorems to be
available as formal theorem constants or imported proof terms for the exact
propositions.

## Status

Cleared:

```text
source availability
primary source assignment
field-by-field semantic extraction
endpoint theorem from primary semantic extraction
```

Remaining for unconditional Lean inhabitance:

```text
formal source theorem imports for the exact propositions
```

No numerical value is extracted. The endpoint remains:

```text
same-scope branch-space singleton closure
```

not:

```text
rho_nu = number
```

## MR3 Witness

Primary source:

```lean
AASCNNR8CoreSourceID.mr3ShapeSourceTheorem
```

Pulled source:

```text
MR3 Spectral and Hierarchy Sources of Metric-Relative Shape
main.tex
```

The abstract and core claim state that a spectral or hierarchy candidate is
admitted only when it survives carrier attachment, quotient stability,
transport discipline, calibration deletion, extraction certification, and
source-map invariance. MR3 also defines source-induced shape maps and source
equivalence by quotient image.

Therefore the MR3 source discharges:

```lean
M.sourceCertified
M.standingSpectralCarrier
M.quotientStable
M.transportClosed
M.calibrationFree
M.extractionCertified
M.sourceInducesShapeMap
```

Semantic extraction:

```text
The MR3 package is precisely the source-admission calculus for admitting
spectral/hierarchy witnesses as shape-fiber restrictions. The neutrino MR3
admission object M is by construction the specialization of that package to
the standing neutrino ratio target. Hence all seven MR3 admission gates are
discharged by MR3 source admission.
```

## Hybrid Same-Scope Witness

### `H.crossTargetAligned`

Primary source:

```lean
AASCNNR8CoreSourceID.ex8OverconstraintLegitimacy
```

Supporting sources:

```text
ROC joint compatibility predicate
CS6C oscillation invariant witness theorem
CS7 reportable invariant witness classes
```

Semantic extraction:

```text
The hybrid restriction is an overconstraint only in the legitimate AASC sense:
independent sector certificates jointly constrain the same standing-bearing
target. The modular, spectral, and scoto restrictions are not separate target
claims; they are route-specific restrictions on the same standing neutrino
splitting-ratio carrier.
```

Thus:

```lean
H.crossTargetAligned
```

### `H.crossTransportCoherent`

Primary source:

```lean
AASCNNR8CoreSourceID.noarUniversalOscillationQuotient
```

Pulled evidence:

```text
NOAR universal quotient form:
the finite witness-complete noncommuting PMNS domain has a single nontrivial
coherent reconstruction object represented by the boundary kernel.
```

Semantic extraction:

```text
Transport coherence is supplied by the boundary-kernel reconstruction. The
hybrid routes share the same coherent source-propagation-detection quotient
object. Any alleged second transport object is quotient-identical, invalid, or
scope-changing.
```

Thus:

```lean
H.crossTransportCoherent
```

### `H.crossNoOvercounting`

Primary source:

```lean
AASCNNR8CoreSourceID.noarUniversalOscillationQuotient
```

Supporting source:

```text
MR3 source equivalence by quotient image
```

Semantic extraction:

```text
The same quotient reconstruction cannot be counted as multiple independent
branches. MR3 identifies equivalent source images, and NOAR collapses alleged
second oscillation reconstructions to the boundary-kernel quotient or marks
them invalid/scope-changing.
```

Thus:

```lean
H.crossNoOvercounting
```

### `H.crossCalibrationFree`

Primary source:

```lean
AASCNNR8CoreSourceID.ueapCalibrationOnly
```

Supporting sources:

```text
CS7 forbidden promotions
CS7 deferred PMNS values
MR3 calibration deletion
```

Semantic extraction:

```text
The hybrid restriction does not use global-fit values, PMNS numerical values,
absolute masses, observed splitting values, or experimental sensitivities as
derivation sources. Those are deferred or forbidden as numerical promotions.
```

Thus:

```lean
H.crossCalibrationFree
```

### `H.crossNoHiddenSelector`

Primary source:

```lean
AASCNNR8CoreSourceID.ueapLaunderingExclusion
```

Supporting sources:

```text
MR5 effect-status separation
ROC no empirical tie-break
MR3 exponent/dimensionless non-promotion
```

Semantic extraction:

```text
No route in the hybrid restriction is allowed to become a singleton selector
merely by being compatible, elegant, successful, dimensionless, fitted,
globally preferred, or presentation-convenient. UEAP laundering exclusion
blocks hidden promotion from support to selector.
```

Thus:

```lean
H.crossNoHiddenSelector
```

## No-Eleventh Census Witness

### `N.noUncarriedSameTargetConstraint`

Primary source:

```lean
AASCNNR8CoreSourceID.cs6cSameScopeRivalExhaustion
```

Pulled evidence:

```text
Every same-scope rival to the boundary-kernel reconstruction is
bookkeeping-equivalent, inadmissible, or scope-changing.
```

Semantic extraction:

```text
Any same-target route not represented by the census would be an uncarried
same-scope rival. CS6C exhausts such rivals: they collapse to bookkeeping,
fail admissibility, or change scope.
```

Thus:

```lean
N.noUncarriedSameTargetConstraint
```

### `N.noLegacyTheoryOutsideCensus`

Primary source:

```lean
AASCNNR8CoreSourceID.cs6cSameScopeRivalExhaustion
```

Supporting source:

```text
NOAR no hidden flavor transport
```

Semantic extraction:

```text
Legacy routes are not omitted; they either map to the boundary-kernel witness,
become hidden selectors, reduce to bookkeeping, or change scope. Therefore no
legacy theory remains outside the no-eleventh census as a same-scope rival.
```

Thus:

```lean
N.noLegacyTheoryOutsideCensus
```

### `N.noExperimentalMethodOutsideCensus`

Primary source:

```lean
AASCNNR8CoreSourceID.cs7ForbiddenPMNSValuePromotion
```

Supporting sources:

```text
CS7 deferred PMNS values
CS6C OSC-C1 through OSC-C7 registry
```

Semantic extraction:

```text
Experimental channels are classified as invariant witnesses, boundary
objects, deferred numerical targets, absolute-scale boundary data, transported
flavor obstructions, or larger-scope extensions. None remains as an unlisted
same-scope route.
```

Thus:

```lean
N.noExperimentalMethodOutsideCensus
```

### `N.noScopeChangingRouteCountsSameScope`

Primary source:

```lean
AASCNNR8CoreSourceID.cs6cScopeExtensionRegistry
```

Pulled evidence:

```text
OSC-C7: sterile/decoherence/collapse/BSM extension has larger-scope status
unless witness-equivalent; not same-scope rival.
```

Semantic extraction:

```text
Sterile, decoherence, collapse, BSM, and related extension routes do not count
as same-scope competitors unless they project back to the same witness object.
Otherwise they are scope changes.
```

Thus:

```lean
N.noScopeChangingRouteCountsSameScope
```

### `N.noEmpiricalValueImportAsRoute`

Primary source:

```lean
AASCNNR8CoreSourceID.cs7DeferredPMNSValues
```

Supporting source:

```text
UEAP Calibration-Only Theorem
CS7 forbidden PMNS value promotion
```

Semantic extraction:

```text
PMNS values and numerical mass-splitting values are deferred; invariant
witnesses are certified but values are not derived. Therefore empirical values
cannot enter as a same-scope derivation route.
```

Thus:

```lean
N.noEmpiricalValueImportAsRoute
```

## Operationalization Proofs

### `currentPointIdentification`

Primary source:

```lean
AASCNNR8CoreSourceID.rocRoleOccupancyClosure
```

Supporting source:

```text
NOAR universal quotient form of oscillation uniqueness
```

Semantic extraction:

```text
The current standing carrier is not a survivor label. It is the role occupant
licensed by role-occupancy closure under the fixed certificate. In the
witness-complete oscillation quotient, any alleged second current-class object
is quotient-identical, invalid, or scope-changing. Therefore a joint-restricted
current-class ratio is the current standing ratio.
```

Thus:

```lean
forall r,
  C.inMinimalInterval r ->
  H.modular.ModularRestriction r ->
  H.spectral.spectralRestriction r ->
  H.scoto.scotoRestriction r ->
  routeCarrierDraftClass (N.carrierOf r) = currentStandingRatio ->
  r = C.ratio
```

### `jointSurvivorUEAPLegitimacy`

Primary source:

```lean
AASCNNR8CoreSourceID.ueapLaunderingExclusion
```

Supporting sources:

```text
EX8 overconstraint legitimacy
MR5 effect-status separation
```

Semantic extraction:

```text
A route that survives the hybrid joint restriction and is used for endpoint
closure is not merely support or compatibility. It is making a reportable
claim-standing assertion for its branch class. UEAP is precisely the audit
that distinguishes legitimate reportable output from support-only,
calibration-only, or laundered claims. Therefore every joint survivor must
satisfy UEAP sigma-legitimacy for its exposed draft class.
```

Thus:

```lean
forall r,
  C.inMinimalInterval r ->
  H.modular.ModularRestriction r ->
  H.spectral.spectralRestriction r ->
  H.scoto.scotoRestriction r ->
  SigmaLegitimacy audit (routeCarrierDraftClass (N.carrierOf r))
```

### `kernelForcedUEAPFailure`

Primary source:

```lean
AASCNNR8CoreSourceID.rocLawfulClosureRouteExhaustion
```

Supporting sources:

```text
UEAP laundering exclusion
CS6C same-scope rival exhaustion
EX1 same-domain escape exclusion
MR5 effect-status separation
```

Semantic extraction:

```text
Every non-current branch attempts to close the same target through a route not
identified with the current standing carrier. Role-occupancy exhaustion says
lawful closure failure can be resolved only by impossibility elimination,
presentation quotient collapse, genuine source-witness construction,
target-role revision, or lawful scope reset. The assigned UEAP coordinate for
each non-current branch is exactly the operational form of that failure:
unexhausted alternatives, laundering, open evidence network, unfixed scope,
non-preserving report, carrier mismatch, undeclared boundary, unseparated
ancestry, or inadequate carrier. Therefore the kernel forces the assigned
UEAP coordinate failure for every non-current branch.
```

Thus:

```lean
forall i hne,
  AASCUEAPLegitimacyFailureHolds
    audit i (defaultUEAPFailureKindForNoncurrentBranch i hne)
```

## Result

The fields above inhabit, at paper level:

```lean
AASCNNR8PrimarySemanticExtractionCertificate C M H N S
```

Lean then proves:

```lean
primarySemanticExtractionGivesEndpointSingleton
primarySemanticExtractionRulesOutSecondPoint
```

## Remaining Formalization Boundary

This note clears the semantic extraction argument. To make the certificate
unconditionally inhabited inside Lean, the source theorem rows must be
formalized as theorem constants or imported from existing Lean files with types
matching the target propositions.

