# Release Notes

## v0.1.0

Initial standalone A+ Lean audit archive for `Standard Model Carrier Exhaustion
/ Generation Cardinality`.

- Adds the `GenerationCardinality` Lake target.
- Carries SM-1 through SM-6 as local received ARC layers.
- Mechanizes the open-generation role-occupancy endpoint audit surface,
  including `ClosedGenCert(n) iff n = 3`.
- Preserves the exact PDF/source ZIP/source snapshot under
  `papers/generation-cardinality/`.
- Adds a 53-row formal-environment theorem inventory.
- Adds the local verifier `scripts/check-generation-cardinality-audit.ps1`.
- Keeps numerical value derivations, untyped generation-holonomy import, and
  model-by-model BSM phenomenology outside the release claim.
